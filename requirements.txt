fastapi==0.115.5
uvicorn[standard]==0.37.0
pydantic==2.11.7
pydantic-settings==2.6.1
python-dotenv==1.1.0
python-multipart==0.0.29

# Workflow runtime
langgraph==1.2.0
langgraph-checkpoint==4.1.0
structlog==25.5.0

# Async scheduling (beat-driven periodic jobs: nudges, recovery, timeout sweeps)
celery==5.6.3

# Security
PyJWT==2.12.1

# MCP client (Anthropic fastmcp SDK over Streamable HTTP) + Cloud Run IAM auth
fastmcp>=3.3,<4.0
google-auth>=2.30,<3.0

# Observability
prometheus-client==0.24.1
sentry-sdk==2.42.1

# Infrastructure clients (production adapters; imported lazily)
redis==7.4.0
SQLAlchemy[asyncio]==2.0.49
greenlet==3.5.0
asyncpg==0.31.0
alembic==1.18.4
langgraph-checkpoint-postgres==3.1.0
psycopg[binary]==3.3.4

# Testing
pytest==8.3.4
pytest-asyncio==0.24.0
httpx==0.28.1
aiosqlite==0.22.1

# Lint + type-check (CI uses these; see .github/workflows/ci.yml)
ruff==0.7.4
mypy==1.13.0
