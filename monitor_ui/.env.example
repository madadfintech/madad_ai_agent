# Backend → log_monitor admin token. Required.
# Copy from ~/madad_ai_agent/.env on the staging VM.
ADMIN_API_TOKEN=

# Optional overrides — defaults work for the standard staging setup.
# MONITOR_SSH_HOST=34.18.50.97
# MONITOR_SSH_USER=jathish
# MONITOR_SSH_KEY_PATH=../staging_server_access/jathish_madad_agent_staging
# MONITOR_REMOTE_HOST=127.0.0.1
# MONITOR_REMOTE_PORT=8090
# MONITOR_LOCAL_BIND_PORT=0   # 0 = auto-pick a free port
# MONITOR_UI_DB=../monitor_ui/history.db
